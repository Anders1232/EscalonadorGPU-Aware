#include "Escalonador.h"
#include "Comunicador.h"

/*
A main deve receber como argumento dois inteiros:
	a porta com a qual o programa Java vai enviar mensagens para o programa C
	a porta com a qual o programa C vai enviar mensagens para o programa Java
*/
int main(int argc, char **argv)
{
	//verificar se recebeu os argumentos
	//criar os 2 sockets de comunicação
	//fazer teste simples com o socket(msg de boas vindas)
	//criar a estrutura do comunicador para enviar para o escalonador
	//criar o escalonador
}
